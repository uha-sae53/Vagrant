apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose convert -f docker-compose-commandes.yml -o ../manifests/
    kompose.version: 1.37.0 (fb0539e64)
    service: api_commandes
    type: api
  labels:
    io.kompose.service: apicommandes
  name: apicommandes
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: apicommandes
  template:
    metadata:
      annotations:
        kompose.cmd: kompose convert -f docker-compose-commandes.yml -o ../manifests/
        kompose.version: 1.37.0 (fb0539e64)
        service: api_commandes
        type: api
      labels:
        io.kompose.service: apicommandes
    spec:
      imagePullSecrets:
        - name: ghcr-secret
      initContainers:
        - name: wait-for-db
          image: busybox:latest
          # ✅ CORRECTION : Utiliser le port du service dbcommandes
          command: ['sh', '-c', 'until nc -z dbcommandes 3309; do echo waiting for db; sleep 2; done;']
      containers:
        - image: ghcr.io/uha-sae53/api-commandes:latest
          name: apicommandes
          ports:
            # ✅ CORRECTION : Exposer le port 8003 (celui utilisé par le conteneur)
            - containerPort: 8003
              protocol: TCP
          env:
            - name: DB_COMMANDES_HOST
              value: "dbcommandes"
            - name: DB_COMMANDES_PORT
              value: "3309"
            - name: DB_COMMANDES_NAME
              value: "commandes"
            - name: DB_COMMANDES_USER
              value: "root"
            - name: DB_COMMANDES_PASSWORD
              value: "commandes"
      restartPolicy: Always