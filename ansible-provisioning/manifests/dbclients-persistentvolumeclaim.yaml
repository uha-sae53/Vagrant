apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: dbclients
  name: dbclients
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
