---
# init du cluster
- name: Check si cluster deja init
  stat:
    path: /etc/kubernetes/admin.conf
  register: k8s_initialise

- name: Reset kubeadm si echec precedent
  command: kubeadm reset -f
  when: not k8s_initialise.stat.exists
  ignore_errors: yes

- name: Nettoyer config kubelet
  file:
    path: "{{ item }}"
    state: absent
  loop:
    - /etc/kubernetes/manifests
    - /var/lib/kubelet
    - /etc/cni/net.d
  when: not k8s_initialise.stat.exists
  ignore_errors: yes

- name: Recreer dossiers
  file:
    path: "{{ item }}"
    state: directory
    mode: '0755'
  loop:
    - /etc/kubernetes/manifests
    - /var/lib/kubelet
  when: not k8s_initialise.stat.exists

- name: Restart containerd avant init
  systemd:
    name: containerd
    state: restarted
  when: not k8s_initialise.stat.exists

- name: Restart kubelet avant init
  systemd:
    name: kubelet
    state: restarted
  when: not k8s_initialise.stat.exists
  ignore_errors: yes

- name: Attendre services
  pause:
    seconds: 5
  when: not k8s_initialise.stat.exists

- name: Init cluster k8s
  command: kubeadm init --apiserver-advertise-address=192.168.56.10 --pod-network-cidr=10.244.0.0/16 --v=5
  when: not k8s_initialise.stat.exists
  register: kubeadm_init
  failed_when: false

- name: Afficher logs kubeadm si erreur
  debug:
    msg: "{{ kubeadm_init.stdout_lines }}"
  when: not k8s_initialise.stat.exists and kubeadm_init.rc != 0

- name: Fail si kubeadm init a echoue
  fail:
    msg: "kubeadm init a echoue. Verifiez les logs ci-dessus."
  when: not k8s_initialise.stat.exists and kubeadm_init.rc != 0

- name: Creer dossier .kube vagrant
  file:
    path: /home/vagrant/.kube
    state: directory
    owner: vagrant
    group: vagrant
    mode: '0755'

- name: Copier config kubectl
  copy:
    src: /etc/kubernetes/admin.conf
    dest: /home/vagrant/.kube/config
    remote_src: yes
    owner: vagrant
    group: vagrant
    mode: '0644'

# reseau flannel pour la com entre pods
# CORRECTION: Utiliser shell avec filtre grep pour ignorer les erreurs AlreadyExists
- name: Installer Flannel (idempotent)
  become_user: vagrant
  shell: |
    kubectl apply -f https://github.com/flannel-io/flannel/releases/latest/download/kube-flannel.yml 2>&1 | grep -v "AlreadyExists" || true
  environment:
    KUBECONFIG: /home/vagrant/.kube/config
  register: flannel_install
  changed_when: "'created' in flannel_install.stdout or 'configured' in flannel_install.stdout"


######## CONFIGURATION APRES INIT ########
### Source : https://kubernetes.io/docs/setup/production-environment/tools/kubeadm/create-cluster-kubeadm/ ###

# recuperer token pour les workers
- name: Recuperer token
  shell: kubeadm token list | grep authentication | head -n1 | awk '{print $1}'
  register: k8s_token
  changed_when: false

- name: Recuperer hash CA
  shell: openssl x509 -pubkey -in /etc/kubernetes/pki/ca.crt | openssl rsa -pubin -outform der 2>/dev/null | openssl dgst -sha256 -hex | sed 's/^.* //'
  register: k8s_ca_hash
  changed_when: false

# creer le script de join pour les workers
- name: Generer commande join
  copy:
    content: "kubeadm join 192.168.56.10:6443 --token {{ k8s_token.stdout }} --discovery-token-ca-cert-hash sha256:{{ k8s_ca_hash.stdout }}"
    dest: /vagrant/join.sh
    mode: '0755'

- name: Attendre synchro fichier
  pause:
    seconds: 3

- name: Creer fichier .master-ready
  copy:
    content: "Master initialized at {{ ansible_date_time.iso8601 }}"
    dest: /vagrant/.master-ready

# storage local
# Il permet aux pods de demander du stockage persistant à partir d'un PersistentVolumeClaim (PVC)
- name: Verifier si local-path est deja installe
  shell: kubectl get storageclass local-path 2>/dev/null
  environment:
    KUBECONFIG: /etc/kubernetes/admin.conf
  register: local_path_exists
  failed_when: false
  changed_when: false

- name: Installer local-path provisioner
  shell: |
    kubectl apply -f https://raw.githubusercontent.com/rancher/local-path-provisioner/master/deploy/local-path-storage.yaml 2>&1 | grep -v "AlreadyExists" || true
  environment:
    KUBECONFIG: /etc/kubernetes/admin.conf
  when: local_path_exists.rc != 0
  register: local_path_install
  changed_when: "'created' in local_path_install.stdout"

- name: Attendre que le provisioner soit pret
  command: kubectl wait --for=condition=available --timeout=120s deployment/local-path-provisioner -n local-path-storage
  environment:
    KUBECONFIG: /etc/kubernetes/admin.conf
  when: local_path_exists.rc != 0
  register: provisioner_ready
  retries: 3
  delay: 10
  until: provisioner_ready.rc == 0
  failed_when: false

- name: Definir local-path par defaut
  command: kubectl annotate storageclass local-path storageclass.kubernetes.io/is-default-class=true --overwrite
  environment:
    KUBECONFIG: /etc/kubernetes/admin.conf
  register: annotate_result
  changed_when: "'annotated' in annotate_result.stdout"
  failed_when: false


# configuration du ServiceAccount par defaut
- name: Attendre ServiceAccount default
  command: kubectl get serviceaccount default -n default
  environment:
    KUBECONFIG: /etc/kubernetes/admin.conf
  register: sa_check
  until: sa_check.rc == 0
  retries: 30
  delay: 2
  changed_when: false


# Creer le namespace apps
- name: Creer namespace apps
  command: kubectl create namespace apps
  environment:
    KUBECONFIG: /etc/kubernetes/admin.conf
  register: namespace_apps
  failed_when: false
  changed_when: "'created' in namespace_apps.stdout"

# Appliquer les manifests databases
- name: Appliquer les manifests databases dans apps (idempotent)
  shell: kubectl apply -f /vagrant/manifests/databases/ -n apps --force 2>&1 || true
  environment:
    KUBECONFIG: /etc/kubernetes/admin.conf
  register: db_apply_result
  changed_when: "'created' in db_apply_result.stdout or 'configured' in db_apply_result.stdout"
  failed_when: false